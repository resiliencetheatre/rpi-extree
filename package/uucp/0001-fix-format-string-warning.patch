Description: Fix format string warning that modern compilers diagnose
Origin: Debian compiler-warnings.patch
--- a/uuconv.c
+++ b/uuconv.c
@@ -676,7 +676,7 @@
     {
       if ((*pz)[0] != '-' && pz != pzarg)
 	fprintf (e, " ");
-      fprintf (e, *pz);
+      fprintf (e, "%s", *pz);
     }
 }
